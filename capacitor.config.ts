import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gobervial.app',
  appName: 'GoberVial',
  webDir: 'dist/gob-vial-app/browser'
};

export default config;
